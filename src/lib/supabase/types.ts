// Placeholder database types.
// Regenerated from the live schema after the first migration runs:
//   npx supabase gen types typescript --db-url "$SUPABASE_DB_URL" > src/lib/supabase/types.ts
// Until then, queries are loosely typed.
export type Database = any;
